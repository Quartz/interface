import { ArgsTable, Canvas, Story } from '@storybook/addon-docs/blocks';
import Icon from './Icon';

<Meta title="Content/Icon" component={Icon} />

# Icon

Renders an icon from the icon library.

## Usage guidelines

- **Do** update the `Icon` component if you make improvements that other components could benefit from.
- **Do not** use the `Icon` component.

## Props

<Canvas>
	<Story
		args={{}}
		name="Default"
	>
		{args => <Icon {...args} />}
	</Story>
</Canvas>

<ArgsTable story="Default" />

## Library

<Canvas>
	<Story
		args={{
			icons: [
				'apple',
				'facebook',
				'linkedin',
				'twitter',
				'youtube',
			]
		}}
		name="Library"
	>
		{
			args => (
				<table border="1" cellpadding="10">
					{
						args.icons.map( icon => (
							<tr>
								<td>{icon}</td>
								<td><Icon name={icon} /></td>
							</tr>
						) )
					}
				</table>
			)
		}
	</Story>
</Canvas>
