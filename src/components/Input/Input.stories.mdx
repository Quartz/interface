import { ArgsTable, Canvas, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import Input from './Input';

<Meta title="Input" component={Input} />

# Input

A text field that accepts and validates user input.

## Usage Guidelines

## Props

<Canvas>
	<Story
		name="Default"
		args={{
			placeholder: 'Search'
		}}
	>
		{ args => <Input {...args} /> }
	</Story>
</Canvas>

<ArgsTable story="Default" />

## Stories

<Canvas>
	<Story
		name="With Required Fields"
		args={{
			buttonProps: {
				children: <Button>Submit</Button>,
				onClick: action( 'clicked' ),
			},
			id: 'email',
			label: 'Email',
			placeholder: 'example@qz.com',
			required: true,
			type: 'email',
		}}
	>
		{ args => (
			<form>
				<Input {...args} />
			</form>
		) }
	</Story>
	<Story
		name="With Required Fields (invalid)"
		args={{
			buttonProps: {
				children: <Button>Submit</Button>,
				onClick: action( 'clicked' ),
			},
			id: 'email',
			label: 'Email',
			placeholder: 'example@qz.com',
			required: true,
			type: 'email',
			status: 'error',
			subtext: 'Please enter a valid email'
		}}
	>
		{ args => (
			<form>
				<Input {...args} />
			</form>
		) }
	</Story>
</Canvas>

<Canvas>
	<Story
		name="With Checkmark"
		args={{
			id: 'promo',
			label: 'Promo Code',
			maxLength: 20,
			status: 'confirmed',
			statusText: '40% Off',
			subtext: 'Promo codes only apply to the yearly plan',
			defaultValue: 'QZFALLSALE',
		}}
	>
		{ args => <Input {...args} /> }
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Loading"
		args={{
			status: 'loading',
		}}
	>
		{ args => <Input {...args} /> }
	</Story>
</Canvas>
